var createPop = function(type,text){
    var o = new Object();
    o.content = text;
    o.show = function(){
        // 显示方法
    }
    if(type === 'alert'){

    }
    if(type === 'prompt'){

    }
    if(type === 'confirm'){

    }
    return o;
}